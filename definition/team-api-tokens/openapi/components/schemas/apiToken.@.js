// Since it has the same name it won't matter, but we re-export it in case
// the user-api-tokens is not enabled.
export { default } from '../../../../user-api-tokens/openapi/components/schemas/apiToken.@.js'
